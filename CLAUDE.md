# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Overview

This repository currently contains a single Python script `hello.py` that prints "Hello Claude Code". There is no established project structure, build system, test suite, or linting configuration.

## Development Environment

- **Language**: Python 3
- **File**: `hello.py` - simple print statement

## Commands

There are no existing build, lint, or test commands. To run the script:

```bash
python hello.py
```

## Architecture

No architecture beyond a single standalone script.

## Future Development

If you plan to add more Python code, consider establishing standard Python project practices:
- Add a `requirements.txt` or `pyproject.toml` for dependencies
- Set up a virtual environment
- Add a test suite using `pytest`
- Configure linting with `black` and `flake8`
- Use `git` for version control (this directory is not currently a git repository)