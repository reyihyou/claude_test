# DeepSeek API配置
DEEPSEEK_API_KEY=sk-your-actual-api-key-here

# 服务器配置
PORT=3000
NODE_ENV=development

# 可选：CORS配置（允许的前端地址）
# CORS_ORIGIN=http://localhost:8000,http://127.0.0.1:8000

# 注意：
# 1. 将DEEPSEEK_API_KEY替换为你的真实API密钥
# 2. 在生产环境中，确保NODE_ENV=production
# 3. 本文件仅作为示例，实际配置请复制为.env文件